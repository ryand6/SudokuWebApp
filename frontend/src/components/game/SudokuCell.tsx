import type { CellState } from "@/pages/GamePage";
import React from "react";

const SudokuCell = React.memo(function SudokuCell({cellState}: {cellState: CellState}) {
    return (
        <div>

        </div>
    )
})

export default SudokuCell;