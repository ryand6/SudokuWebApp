import type { GamePlayers } from "@/types/game/GameTypes"

export function GameResults({
    userId,
    players
}: {
    userId: number,
    players: GamePlayers
}) {
    return (
        <>
        </>
    )
}